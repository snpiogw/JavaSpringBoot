<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sample.common.dao.mapper.TaskMapper">
   <select id="findByUsername" resultType="sample.common.dao.entity.Task">
    SELECT id, username, title, content, name, 
           start_date AS startDate, end_date AS endDate, 
           created_at AS createdAt, updated_at AS updatedAt 
    FROM tasks 
    WHERE username = #{username} 
    ORDER BY id DESC
    LIMIT 10 OFFSET #{offset}
</select>
    
    <select id="findByIdAndUsername" resultType="sample.common.dao.entity.Task">
        SELECT id, username, title, content, name, 
               start_date AS startDate, end_date AS endDate, 
               created_at AS createdAt, updated_at AS updatedAt 
        FROM tasks 
        WHERE id = #{id} AND username = #{username}
    </select>
    
    <insert id="insertTask" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tasks (username, title, content, name, start_date, end_date)
        VALUES (#{username}, #{title}, #{content}, #{name}, #{startDate}, #{endDate})
    </insert>
    
    <update id="updateTask">
    UPDATE tasks
       SET title = #{title},
           content = #{content},
           name = #{name},
           start_date = #{startDate},
           end_date = #{endDate},
           updated_at = now()
     WHERE id = #{id}
       AND username = #{username}
    </update>
    
    <delete id="deleteTask">
        DELETE FROM tasks WHERE id = #{id} AND username = #{username}
    </delete>
</mapper>